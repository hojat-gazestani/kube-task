apiVersion: v1
kind: Service
metadata:
  name: {{ include "My-phpmyadmin.fullname" . }}
  labels:
    {{- include "My-phpmyadmin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "My-phpmyadmin.selectorLabels" . | nindent 4 }}
