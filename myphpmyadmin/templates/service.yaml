apiVersion: v1
kind: Service
metadata:
  name: {{ include "myphpmyadmin.fullname" . }}
  labels:
    {{- include "myphpmyadmin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myphpmyadmin.selectorLabels" . | nindent 4 }}
